# default-meshpi-home

## 目标与验收条件
- 默认存储根目录改为用户主目录下的 `.meshpi`，保留绝对路径 `MESHPI_HOME` 覆盖。
- README 定位面向多端，明确当前平台验证范围。
- 原有数据不被自动覆盖或删除，提供旧目录使用说明。

## 当前状态
- 状态：待交接（实现与本地验证完成，待 PR 合并）
- 工作分支：`codex/default-meshpi-home`
- PR：待创建
- 最近更新时间：2026-09-07（Asia/Shanghai）
- 最近工作设备：本地 macOS
- 最近验证的代码提交：基于 `5bb86ef31235ce6f4c202b5e32da96f29ec35f31` 的本次变更；具体实现提交见本文件 Git 历史。

## 已完成
- `src/paths.mjs` 使用新的默认目录；测试覆盖默认目录、权限、旧目录保留、覆盖路径与相对路径拒绝。
- README、AGENTS.md 和设计决策同步多端定位及存储规则。

## 关键决定
- 不自动迁移个人数据；旧目录可通过 `MESHPI_HOME` 继续使用。
- 见 [默认本地目录与多端定位](../decisions.md#默认本地目录与多端定位2026-09-07)。

## 验证
- macOS、Node v25.2.1：`npm run check`、`npm test`（9 项，包含临时目录安装测试）、`npm start -- --help`、`git diff --check` 均通过。
- 新目录测试使用临时 HOME 和合成数据，未读取个人凭据。
- 未验证：Windows 原生环境；此次未更新个人全局安装或迁移旧数据。

## 下一步
1. 检查远端 CI，通过 PR 合并。
2. 使用新版前重新安装；根据需要选择新默认目录或显式使用旧目录。

## 风险与待确认
- 新默认目录不会找到旧目录中的凭据和历史；README 已说明。
- 首次切换分支受沙箱写权限限制，授权后创建本地 main 并从 origin/main 建立迭代分支成功。

## 最近交接
- 本轮改动：默认目录及多端文档，回归测试通过。
- 尚未完成：PR 合并；全局安装和个人数据迁移不在本轮操作内。
- 本机未同步内容：提交推送前为上述变更，实际同步结果以交付回复和远端为准。
