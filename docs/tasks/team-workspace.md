# team-workspace

## 目标与状态
- 状态：进行中；分支：codex/team-workspace；基线：f26b9aa。
- 只有平台管理员可以创建团队并指定负责人；负责人管理成员、邀请与项目，成员按团队隔离。
- 统一用户和管理入口，见 docs/teams.md。维护者明确要求规范入口。
- 最近更新时间：2026-09-08 Asia/Shanghai；设备：本地 macOS。

## 实现与决定
- Node.js/PostgreSQL 现有栈，无新依赖。服务端 0.3.0，CLI 0.2.0。
- 新增 003-team-invites 与 004-device-sessions，成员关系与项目沿用现有表。创建和邀请核销事务化，团队锁串行化权限与成员变更。
- 工作台 /，账号 /account；管理 /admin/teams、/admin/invites、/admin/directory。
- 普通用户无团队创建权限；负责人不能被移除。新增管理员用户启停/密码重设/设备撤销、团队名称/负责人调整/归档，负责人项目改名/归档；CLI 登录、设备注册、列表与项目绑定。不包含研究记录共享或 Memory。

## 验证
- 本地服务端 check 通过，11 项离线测试通过，3 项数据库测试因无 PostgreSQL 跳过。
- 8e54e06 的 GitHub PostgreSQL 15 与四组系统/Node CI 通过；合成页面已验证打开团队和创建项目。
- 19f97ed 的 GitHub PostgreSQL 15、发布 verify、四组系统/Node CI 全部通过，包含 HTTPS + 真实 CLI 进程登录/绑定/撤销/禁用/改密端到端测试。
- 本机客户端 10 项（含安装）、服务端 11 项离线测试通过；4 项数据库测试本机跳过。
- 最终代码验证提交 12829b5584273fd64e582e3619e21b8f8a72699f：PostgreSQL 15（含管理员 HTTP、TLS+CLI 端到端）、部署 verify、macOS/Linux Node 22.19/24 全部通过。
- 合成浏览器预览已检查工作台、团队选择、项目提交、生成邀请码、用户列表及设备面板。未把合成 UI 预览当作生产验收。
- 人工升级脚本语法检查通过，尚未在生产执行。

## 下一步与发布限制
- PR #10 为草稿，代码与 CI 已就绪；管理员执行 docs/team-service.md 的 admin-upgrade.sh，使用 12829b5584273fd64e582e3619e21b8f8a72699f 归档。
- 生产仍为 0.2.1；新增迁移必须先人工执行并部署基线，普通 workflow 不执行迁移。
- 服务器已有用户，之前空库跳过备份仅针对 002，不延续为空库假设。
- 本地无 ECS 管理连接；不能提取 GitHub 密钥或绕过服务器迁移保护。

## 完整目标补充
- 维护者要求服务端用户、团队、成员、设备、项目管理与 CLI 配套完成，供同学实际使用。
- 设备与 CLI 真数据库端到端验证、交付指南已完成；仍需生产迁移、自动部署与真实用户/设备上线验收。不能只以代码和 CI 作为完整目标完成依据。
