{
  "name": "pimesh-repository",
  "private": true,
  "type": "module",
  "scripts": {
    "deps": "npm ci --prefix apps/cli --ignore-scripts && npm ci --prefix apps/server --ignore-scripts",
    "start": "npm start --prefix apps/cli --",
    "start:server": "npm start --prefix apps/server",
    "check": "npm run check --prefix apps/cli && npm run check --prefix apps/server",
    "test": "npm test --prefix apps/cli && npm test --prefix apps/server",
    "test:install": "npm run test:install --prefix apps/cli"
  }
}
